# LangChain family - aligned to 0.3.3 series (all compatible)
langchain==0.3.3
langchain-core==0.3.10          # >=0.3.10 required by langchain 0.3.3
langchain-community==0.3.2
langchain-text-splitters==0.3.0
langchain-groq==0.2.0
langchain-pinecone==0.2.0

# Pinecone core client
pinecone-client==5.0.1

# Embeddings model
sentence-transformers==3.1.1

# Flask + production server
flask==3.0.3
gunicorn==22.0.0

# Utilities
python-dotenv==1.0.1
PyPDF2==3.0.1

# Stable dependencies to avoid build/compilation issues
pydantic==2.9.2
pydantic-core==2.23.4
pyyaml==6.0.1
